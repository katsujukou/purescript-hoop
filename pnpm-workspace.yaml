packages:
  - './runtime/**'
# esbuild の postinstall は bin をネイティブバイナリに差し替える高速化だが、
# pnpm の bin シム(node 経由で実行)と相性が悪いため無効のままにする。
# JS ラッパー経由でもネイティブバイナリ(@esbuild/darwin-arm64 等)が使われる。
allowBuilds:
  esbuild: false
