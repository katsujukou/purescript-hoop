packages:
  - './runtime/**'
# esbuild's postinstall is an optimization that swaps the bin for a native binary,
# but it plays badly with pnpm's bin shims (which run via node), so keep it disabled.
# The native binary (@esbuild/darwin-arm64 etc.) is still used via the JS wrapper.
allowBuilds:
  esbuild: false
